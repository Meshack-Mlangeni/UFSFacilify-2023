<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/main_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/main_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyRecApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.book.EditFacilityActivity"
            android:exported="false" />
        <activity
            android:name=".ui.book.CancelActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="mncedisi.mlangeni.myrecapp.ui.book.CancelActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.book.SuccessActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="mncedisi.mlangeni.myrecapp.ui.book.SuccessActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.book.MakeBookingActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="mncedisi.mlangeni.myrecapp.ui.book.MakeBookingActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="mncedisi.mlangeni.myrecapp.MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>